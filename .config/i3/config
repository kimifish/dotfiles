# vim: fdm=marker
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!


# ================= SHARED SETTINGS ========================
#
set $mod Mod4
set $modcodeR 134
set $modcodeL 133
set $exorf $HOME/kimiside@gmail.com/bin/exec_or_focus.sh

# Fonts {{{
#
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
#font pango:DejaVu Sans Mono 6
#font pango:NotoMono Nerd Font Mono Book 6
font pango:Play-Regular 10
#font pango:DejaVu Sans Mono 8
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# }}}

# Mouse {{{

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

focus_follows_mouse no           

# }}}

# Gaps and borders {{{
for_window [class=".*"] border pixel 1
# }}}

# Colors {{{
set $background #a0a0a0
set $foreground #ffffff
set $gray       #d0d0d0

#set $primary	#588c7e
#set $secondary	#e9ce49
#set $tertiary	#f2ae72
#set $additional	#d96459

# green-gray
# set $primary    #82b74b
# set $secondary  #405d27
# set $tertiary   #3e4444
# set $additional #c1946a

set_from_resource $primary    $PAL_TMUX_COL1 #82b74b
set_from_resource $secondary  $PAL_TMUX_COL0 #405d27
set_from_resource $tertiary   $PAL_TMUX_COL2 #3e4444
set_from_resource $additional $PAL_TMUX_COL3 #c1946a
#
set $warning    #E7176A

set $bg-color #2f343f
set $bg2-color #3f343f
set $bg3-color #5f343f
set $text-color #f3f4f5

 # colors                BORDER      BACKGROUND TEXT        INDICATOR
client.focused          $primary    $primary   $foreground $primary
client.focused_inactive $secondary  $secondary $background $secondary
client.unfocused        $tertiary   $tertiary  $background $tertiary
client.urgent           $gray       $gray      $foreground $gray  
 
# }}}

# Windows {{{

# Focus on window activation
# (smart | urgent | focus | none)
focus_on_window_activation focus

popup_during_fullscreen leave_fullscreen

# for window class run xprop in terminal
# Threshold max window size, depends on your display size
#floating_maximum_size 1920 1080
# Tell every window to behave centered.

# for_window [window_role="floating terminal"] floating enable
# for_window [window_role="floating terminal"] resize set 660 440
for_window [class="floating terminal"] floating enable
for_window [class="floating terminal"] resize set 960 940
# for_window [floating] resize set 660 440
# for_window [class="vlc"] floating enable

# for_window [window_role="htop"] floating enable
# for_window [window_role="htop"] resize set 660 760
for_window [class="htop"] floating enable
for_window [class="htop"] resize set 660 760

# for_window [window_role="vifm"] floating enable 
# for_window [window_role="vifm"] resize set 960 640
for_window [class="vifm"] floating enable 
for_window [class="vifm"] resize set 960 940

for_window [class="taskwarrior"] floating enable 
for_window [class="taskwarrior"] resize set 1200 800

for_window [class="music"] floating enable 
for_window [class="music"] resize set 1000 800

# for_window [class="dagor2"] floating disable
# for_window [class="Totalcmd.exe"] floating enable
 
# for_window [title="kimiHome"] floating enable
# for_window [title="kimiHome"] resize set 640 960

# for_window [title="https://www.youtube.com -"] floating disable
# for_window [title="https://www.youtube.com -"] resize set height 360 px

for_window [title="Картинка в картинке"] sticky enable
# for_window [class="vlc"] sticky enable
for_window [class="hollow_knight"] fullscreen enable
#for_window [class="(?)Steam"] fullscreen enable
#
#Должен быть последним, иначе центра не получается
for_window [class=".*"] move position center
# }}}

# Window binds ---------------------- {{{
# kill focused window
bindsym $mod+Shift+q kill

# If somehow a window dare to display outside the display,
# force it with the following keybinding to center its
# position
bindsym $mod+c move absolute position center

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym Control+h nop
bindsym Control+j nop
bindsym Control+k nop
bindsym Control+l nop

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+underscore split h

# split in vertical orientation
# bindsym $mod+backslash split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+w floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

# sticky window enable
bindsym $mod+Shift+s sticky toggle
# }}}

# Positioning apps {{{
#assign [class="Firefox"] 1
#assign [class="Chromium"] 1
#assign [class="qbittorrent"] 1
#assign [class="hollow_knight"] 2
#assign [class="csgo"] 2
#assign [class="(?)Steam"] 2
#assign [class="Thunderbird"] 4
#assign [class="telegram"] 6
#assign [title="^\d+ hidden tabs -.*$"] 7
#assign [class="dagor2"] 2
# }}}

# Apps binds {{{

# vifm
bindsym $mod+semicolon exec ~/.config/rofi/tlaunchers/type-2/combilauncher.sh
# vim-input
bindsym $mod+v exec clipcat-menu
bindsym $mod+Shift+v exec vim-input-field.sh
# tasksh
#bindsym $mod+n exec i3-sensible-terminal --class="vifm" --geometry=160x45 "TERM=tmux-256color tw-tui.AppImage"
bindsym $mod+n exec i3-sensible-terminal --class="taskwarrior" "tw-tui.AppImage"
# flameshot
bindsym $mod+Print exec flameshot gui 
# dmenu-desktop
bindsym $mod+Shift+d exec --no-startup-id i3-dmenu-desktop #dmenu_run
# dmenu
bindsym $mod+d exec --no-startup-id dmenu_run
# rofi run
#bindsym $mod+semicolon exec rofi -show run -lines 20 -eh 1.3 -opacity "90" -bw 0 -bc "$bg-color" -bg "$bg-color" -fg "$text-color" -hlbg "$text-color" -hlfg "$bg-color"
# rofi drun
bindsym $mod+Shift+semicolon exec rofi -show drun -modi drun -lines 20 -eh 1.3 -opacity "90" -bw 0 -bc "$bg2-color" -bg "$bg2-color" -fg "$text-color" -hlbg "$text-color" -hlfg "$bg2-color" 
# rofi window
bindsym $mod+apostrophe exec rofi -show window -modi window -lines 20 -eh 1.3 -opacity "90" -bw 0 -bc "$bg3-color" -bg "$bg3-color" -fg "$text-color" -hlbg "$text-color" -hlfg "$bg3-color" 
# telegram
bindsym $mod+Control+t exec $exorf telegram
#bindsym $mod+Control+f exec $exorf Firefox
#bindsym $mod+Control+m exec $exorf thunderbird

# }}}

# Bot binds {{{

bindsym $mod+minus exec $HOME/.config/i3blocks/volume -5
bindsym $mod+equal exec $HOME/.config/i3blocks/volume +5
bindsym $mod+F9 exec bot prev
bindsym $mod+F12 exec bot next
bindsym $mod+comma exec bot seek "-7"
bindsym $mod+period exec bot seek 7
bindsym $mod+slash exec bot pp
#bindsym $mod+F9 exec mpc prev
#bindsym $mod+F12 exec mpc next
#bindsym $mod+F11 exec mpc toggle
#bindsym $mod+F10 exec mpc seek -7
#
# Multimedia keys binds 
bindsym XF86AudioRaiseVolume exec $HOME/.config/i3blocks/volume +5 #increase sound volume
bindsym XF86AudioLowerVolume exec $HOME/.config/i3blocks/volume -5
bindsym XF86AudioMute exec bot mute_if # mute sound
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
bindsym XF86AudioPlay exec bot pp
bindsym XF86AudioPause exec bot pause
bindsym XF86AudioNext exec bot next
bindsym XF86AudioPrev exec bot prev
bindsym XF86HomePage exec "firefox --new-window http://192.168.1.150:8080/basicui/app&sitemap=kimihome --window-size 640,960"
bindsym XF86Explorer exec i3-sensible-terminal --role="floating terminal" --command="tmux new-session" --hide-menubar
# }}}

# Other binds {{{                                                              
# in case of layout change event - send signal to i3blocks
bindsym --release ISO_Next_Group exec pkill -RTMIN+1 i3blocks

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# lock screen
#bindsym Control+Shift+PgUp exec "terminology -e vifm"
bindsym $mod+Pause exec screenlock lock
#bindsym $mod+Control+Pause exec ". $HOME/bin/autolock.sh toggle"

bindsym $mod+Shift+slash exec i3-sensible-terminal --class=vifm -e ~/bin/i3-binds

# bindsym $mod+i exec kitty --class="vifm" "tmux" "new-session" "-A" "-s" "AI"
bindsym $mod+i exec kitty --class="vifm" "tmuxinator" "I" # AI Start


bindsym --release $mod+Shift+Pause exec monitorcontrol off
# }}}

# Resize mode {{{
# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 30 px
        bindsym j resize grow height 30 px
        bindsym k resize shrink height 30 px
        bindsym l resize grow width 30 px

        # same bindings, but for the arrow keys
        #bindsym Left resize shrink width 3 px or 3 ppt
        #bindsym Left resize shrink width 3 px or 3 ppt
        #bindsym Down resize grow height 3 px or 3 ppt
        #bindsym Up resize shrink height 3 px or 3 ppt
        bindsym Right resize grow width 30 px
        bindsym Down resize grow height 30 px
        bindsym Up resize shrink height 30 px
        #bindsym Right resize grow width 30 px

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# }}}
#
# Launcher mode {{{
#Press $mod+o followed by either f, t, Escape or Return to launch firefox,
# thunderbird or return to the default mode, respectively.
set $mode_launcher Launch: [f]irefox [t]hunderbird
bindsym $mod+o mode "$mode_launcher"

mode "$mode_launcher" {
    bindsym f exec $exorf firefox
    bindsym m exec $exorf thunderbird
    bindsym j exec $exorf jb-toolbox
    bindsym t exec $exorf telegram
    bindsym q exec $exorf qbittorrent


    bindsym Escape mode "default"
    bindsym Return mode "default"
} 
#}}}

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
	#output HDMI-0
	#font pango:DejaVu Sans Mono 6
	#separator_symbol "〉"
        #status_command i3blocks
        #tray_output HDMI-0
#}
                     
# Startup autorun {{{
exec --no-startup-id "setxkbmap -layout kimi,ru -variant latin, -option grp:caps_toggle -option grp_led:scroll -option lv5:ralt_switch -option lv3:menu_switch"
# }}}

include ~/.config/i3/`hostname`.d/*.conf

# Должно быть, обои стоит ставить сюда, иначе рвутся
# Setting wallpaper
# exec $HOME/bin/set_random_wallpaper.sh "$HOME/kimiside@gmail.com/Photos/Wallpapers/Cowboy Bebop"
exec $HOME/bin/set_random_wallpaper.sh "$HOME/kimiside@gmail.com/Photos/Wallpapers/ArtOfBaka"

